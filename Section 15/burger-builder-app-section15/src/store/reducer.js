import { BurgerBuilderActions as actionTypes } from './actions';

const INITIAL_STATE = {
	ingredients: null,
	totalPrice: 3,
};

const reducer = (state = INITIAL_STATE, action) => {
	return state;
};

export default reducer;
