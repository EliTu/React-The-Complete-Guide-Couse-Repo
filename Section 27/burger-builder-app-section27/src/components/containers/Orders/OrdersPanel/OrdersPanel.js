import React from 'react';
import Input from '../../../UI/Input/Input';
import styles from './OrdersPanel.module.css';

const OrdersPanel = () => {
	// CSS Modules styles:
	const { OrdersPanel } = styles;

	return (
		<div className={OrdersPanel}>
			<input />
		</div>
	);
};

export default OrdersPanel;
