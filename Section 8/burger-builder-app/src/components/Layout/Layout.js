import React from 'react';

const Layout = props => {
	return (
		<>
			<div>
				{/* Toolbar */}
				{/* SideDrawer */}
				{/* BackDrop */}
			</div>
			<main>{props.children}</main>
		</>
	);
};

export default Layout;
